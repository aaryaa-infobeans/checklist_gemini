services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    volumes:
      - ./frontend:/app:delegated
    ports:
      - "3001:80"
    depends_on:
      - backend
    environment:
      NODE_ENV: development
    restart: unless-stopped
